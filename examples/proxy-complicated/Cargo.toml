[workspace]

[package]
name = "proxy-complicated"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
poprako-orchestra = { path = "../..", features = ["macro", "oper_ext"] }
futures = "0.3"
