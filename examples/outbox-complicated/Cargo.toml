[workspace]

[package]
name = "outbox-complicated"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
poprako-orchestra = { path = "../.." }
futures = "0.3.32"
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres"] }
tokio = { version = "1", features = ["full"] }
